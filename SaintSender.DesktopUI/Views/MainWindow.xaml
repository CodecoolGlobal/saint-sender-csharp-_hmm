<Window x:Class="SaintSender.DesktopUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SaintSender.DesktopUI"
        mc:Ignorable="d"
        Title="SaintSender" Height="450" Width="800">
    <Grid>
        <Button Name="LoginBtn" Content="Login" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Margin="709,10,0,0" Click="LoginBtn_Click"/>
        <ListView ItemsSource="{Binding EmailsToShow}" Height="300" Margin="0,110,-0.4,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Sender" DisplayMemberBinding="{Binding Path=Sender}" Width="130"/>
                    <GridViewColumn Header="Subject" DisplayMemberBinding="{Binding Path=Subject}" Width="250"/>
                    <GridViewColumn Header="Date" DisplayMemberBinding="{Binding Path=Date}" Width="160"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
